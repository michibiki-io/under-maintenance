apiVersion: v1
kind: Service
metadata:
  name: {{ include "under-maintenance.fullname" . }}
  labels:
    {{- include "under-maintenance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "under-maintenance.selectorLabels" . | nindent 4 }}
